{
  "name": "coda-game-client",
  "version": "0.1.0",
  "private": true,
  "description": "Static GitHub Pages client for the Coda board game.",
  "scripts": {
    "build:css": "tailwindcss -i ./src.css -o ./styles.css --minify",
    "build": "npm run build:css",
    "serve": "npx http-server . -p 4173 -c-1",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@tailwindcss/cli": "^4.3.3",
    "http-server": "^14.1.1",
    "tailwindcss": "^4.3.3"
  }
}
